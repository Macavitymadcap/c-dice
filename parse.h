#ifndef PARSE_H
#define PARSE_H

/**
* Determine whether a string is an integer of one or more digits.
*
* @param input (char *) - The string to be checked.
* @returns (bool) - Is the given string an integer.
*/
bool isNumber(char *input);

#endif